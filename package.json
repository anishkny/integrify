{
  "name": "integrify",
  "version": "0.0.2",
  "scripts": {
    "lint": "tslint --project ./tsconfig.json",
    "build": "npm run lint && tsc"
  },
  "devDependencies": {
    "prettier": "^1.15.3",
    "tslint": "^5.12.1",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  }
}
