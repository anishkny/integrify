{
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^6.5.0",
    "firebase-functions": "^2.1.0"
  },
  "engines": {
    "node": "8"
  }
}
